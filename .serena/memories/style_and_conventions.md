TypeScript strict mode is required. Shared API types should be defined in `packages/shared/src/index.ts`. Server only exposes fixed business APIs and must not accept arbitrary shell commands or raw project paths for Skills/MCP operations. CLI calls must use `execa(command, args)` instead of shell-string concatenation. Logs must redact token/key/secret/password values. MVP-first implementation is preferred over heavy abstraction.